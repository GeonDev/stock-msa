server:
  port: 8083

spring:
  application:
    name: stock-stock
  profiles:
    active: local
  datasource:
    url: jdbc:mysql://localhost:3308/stock_stock?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: user
    password: password
    jpa:
      hibernate:
        ddl-auto: validate
  
  eureka:
    client:
      service-url:
        defaultZone: http://localhost:8761/eureka
  